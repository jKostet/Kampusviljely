<%= form_for(@bag) do |f| %>
  <% if @bag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bag.errors.count, "error") %> prohibited this bag from being saved:</h2>

      <ul>
      <% @bag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :owner %><br>
    <%= f.text_field :owner %>
  </div>
  <div class="field">
    <%= f.label :co_owners %><br>
    <%= f.text_field :co_owners %>
  </div>
  <div class="field">
    <%= f.label :site_id %><br>
    <%= f.number_field :site_id %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
